extends layout

block content
  h1= title
  p Welcome to #{title}
  script(src="/socket.io/socket.io.js")
  script.
    var socket = io();
    socket.on('socketToMe', function (data) {
      console.log(data);
    });

    socket.on('encoding_status', function (data) {
      console.log(data);
    });
